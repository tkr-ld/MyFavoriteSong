<% if setlists.any? %>
  <table class="table">
  <% setlists.each do |setlist| %>
    <tr class="text-left">
      <td><%= link_to setlist_path(setlist) do %> <span class=" glyphicon glyphicon-music"></span><%= setlist.title%><% end %></td>
    </tr>
  <% end %>
  </table>
<% end %>