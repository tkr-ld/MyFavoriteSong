<div class="row">
  <div class="col-xs-8">
    <ul>
      <li><%= @setlist.title %></li>
      <li><%= @musician.name %></li>
      <li><%= @setlist.date %></li>
      <li><%= @setlist.place %></li>
    </ul>
    
    <h5><%= link_to 'edit Setlist', edit_setlist_path(setlist_id: @setlist.id) %></h5> <!-- 引数渡す必要ないかも-->
      <% @songs.each do |song| %>
        <li class="media"><%= song.title %></li>
      <% end %>
  </div>
  
</div>